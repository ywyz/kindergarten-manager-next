# I4 F1/F2 修复复审（2026-09-24）

## 结论与基线

本轮窄范围复审未发现 F1/F2 的剩余代码问题，可以进入 I5 Word 导出实施规格准备。不是全系统无缺陷证明，也不表示真实浏览器交互已验收。

用户要求审阅最新修复；实际 HEAD 仍为 `3120a2c4e9ebfac66439445ad54e3c5b01e2300d`，本次修复尚未提交。审阅对象为相对该 HEAD 的四个工作区文件：

- `frontend/src/views/WeeklyPlanView.vue`
- `frontend/src/weekly-plan-content.ts`
- `docs/specs/manual-weekly-plan-confirmation.md`
- `docs/bootstrap/architecture-v1-readiness.md`

审阅时两个代码文件的 Git blob hash 分别为 `c239a264d07301609bd4bde5069e74f32f73e21b`、`1ae5f2e902816d1c34315cfad0217b6ad76eb3e6`。旧报告仍描述旧基线下的问题，不覆盖为“当时已通过”。已有未提交内容全部保留。

## F1：已修复原覆盖路径

`doRefresh()` 成功后，仅在无 dirty 输入或真实编辑基线与刷新目标相同时推进 `editBaseVersion`；否则保留旧基线，进入 `refresh_kept_baseline` 的保存冲突。正常刷新仍可用，普通保存被冲突状态阻断，用户可明确选择放弃本地或按服务端基准重提。

Codex 本轮实际复跑上一轮临时脚本 `/tmp/kg-i4-3120-review.cjs`，读取当前组件脚本、使用 Vue 响应式和受控 API 桩：

| 阶段 | 实际结果 |
| --- | --- |
| v1 dirty、复核 v2 后取消 | edit base=1，display=2，本地输入保留 |
| 刷新到 v3 | edit base 仍=1，display=3，服务器仍为管理员主题 |
| 未处理冲突就保存 | API 调用列表只有 refresh(expected=2)，没有 PATCH；服务器主题未被覆盖 |

命令退出码 0。该结果与上一轮同脚本能覆盖管理员主题的结果形成直接对照；它是脚本级状态验证，不是浏览器或 MySQL 验证。

## F2：确认快照展示已补齐

源码确认：`confirmSlotPreviews` / `confirmFocusPreview` 读取 `confirmTarget.content`；新纯 helper 展示完整游戏目标/指导/重点指导以及重点区域上下文/目标/指导/支持策略，空值保留占位，来源版本/标识可辨识。候选仅用于能可靠匹配时的日期显示，不覆盖快照文本。

本轮未新增执行 F2 渲染测试。规格 §12.3 记载的 helper 22 项、真实模板 vnode 18 项、F1 29 项及 typecheck/build 通过属于实施方记录；本轮未独立复跑这些完整集合，不把 vnode 结果称为真实浏览器结果。

## 非阻塞文档备注

规格 §12.3 的“本次改动文件仅上述两个前端文件”应理解并可精确改为“本次业务代码改动仅上述两个前端文件”，因为规格与 readiness 也有修改。此为措辞精度问题，不单独开启代码修复任务。

## 协作和验证边界

使用 [Codex with ChatGPT 技能](/home/ywyz/.codex/skills/codex-with-chatgpt/SKILL.md)，继续[同一 ChatGPT 对话](https://chatgpt.com/g/g-p-6ab3c61acea48191aa003d8a6ab6fb81-kindergarten-manager-next/c/6ab4b626-6c90-83ea-8711-e7b5972c5e39)，本轮任务为 `c2c_924c`。ChatGPT 读取工作区 diff 和源码，明确 F1/F2 未发现剩余代码问题，并指出上述文档措辞。

ChatGPT 没有取得实施方完整 29/22/18 项原始运行输出，也没有重新执行检查；其回复中的旧 execution evidence 不是本轮修复后结果。Codex 本轮复跑证据如上独立列出。`git diff --check` 通过，后端无工作区差异；未重新运行 typecheck/build，未创建数据库、容器、启动服务或做浏览器交互验证，未安装依赖、提交、推送或部署。

## 下一步

为 OpenCode 编写 I5 Word 导出实施规格准备提示词，仅文档交付。将版本选择、权限、模板映射、范围筛选、合并、标红、文件生命周期和必要验证细化；文件上限、清理与生产生成库保持建议/待确认状态。I4 剩余真实浏览器呈现检查单列，不阻塞纯文档准备，也不自动扩大为新一轮完整验收。
