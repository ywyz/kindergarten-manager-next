# I2 实施与验证记录

日期：2026-09-21 至 2026-09-22。

**状态：I2 实施与本轮直接相关验证完成。停止于 I2，未提交、部署或进入后续切片。**

## 执行与授权

业务代码初稿由本地 pi 写入。2026-09-22 用户明确确认 pi 套餐到期，授权改用本地 OpenCode，且不再切回 pi。此后业务代码、迁移、测试及对应界面均由 OpenCode 唯一执行；main 负责协调、源码核对、资源准备、浏览器验证及本记录。

用户随后明确回复“授权数据库与依赖”，授权此前列明的隔离资源：

- 现有 `backend/.venv` 仅新增锁定的 `chinesecalendar==1.11.0`，同步 `uv.lock`，无其他依赖升级。
- 启动现有 `kg-next-i1-mysql-20260921`，实际确认 MySQL 8.4.11 / InnoDB，仅映射 `127.0.0.1:13384`。
- 创建 `kindergarten_test_i2` 和 `kindergarten_test_i2_fresh`，账号 `kg_test_i2` 的 DDL/DML 权限仅覆盖这两库。后者供 main 浏览器验证，前者供自动化集成测试。
- 允许两库测试数据及失败迁移重建、迁移执行、定向测试、临时本地 API/Vite 与浏览器验证；结束后停止服务与容器。

不连接生产；原 `kindergarten_test_i1` 及原卷保留。原有 readiness、I1 规格和未跟踪 I2 规格均保留。未改 `ARCHITECTURE.md`，未暂存、提交、推送、部署；未进入 I3、AI、导出或工作进程。

执行中一次 OpenCode 调试输出暴露了仅限两库的测试连接串。main 立即中止该轮并轮换密码，旧值已失效，日志收紧为私有权限；生产及原 I1 凭据未涉及。

## 已实施范围

- 园所单行资料、班级创建与资料维护；小／中／大班、班名全园唯一、升班保留班级 ID。
- 首次真实分配：目标班级与账号版本复核，真实归属、账号 version 与操作记录同事务提交；不改变 auth_version，不要求教师接受，不撤销既有会话。
- Me、教师列表、本班配置及权限响应，保留既有身份功能。
- 学期闭区间不重叠且不共端点；学期与日历修订、完整逐日快照、复合外键约束当前修订属于本学期。
- 实际默认日历、未知年份、管理员批量例外／撤销、周次与日期解释、显式默认重新导入。
- 持久预览、30 分钟期限、明确确认、操作者绑定、对象／成员／日程／阶段基线、幂等结果、原子事务与审计。
- 无计划阶段配置变更；已有计划阶段对既有配置的依赖门槛。未添加计划／任务占位表、空 API、假成功或调班／停启／接管入口。
- 管理员班级／园所／学期／月历及教师只读页面，真实分配、批量例外、预览恢复。

迁移链：`20260921_i1_identity_reg` → `20260921_i2_classes_assignments` → `20260922_i2_terms_calendar`。

## 实际验证证据

### 数据库迁移与保留

- main 对 `kindergarten_test_i2_fresh` 从空库执行 `.venv/bin/alembic upgrade head`，退出 0；实际查询 head 为 `20260922_i2_terms_calendar`，12 张表全部 InnoDB，当前修订复合外键有两列。
- OpenCode 在 `kindergarten_test_i2` 执行 `bash scripts/i2_mysql_fixture.sh i2`，退出 0。先 I1 迁移和真实夹具，再 I2 迁移；使用迁移前数据库读取的快照比较账号密码哈希、版本／认证版本、会话时间及撤销字段、旧审计字段，并核对新增审计目标字段回填。
- 早期夹具使用写入前微秒值导致与 MySQL 秒级存储不一致，已改为迁移前真实读取，不靠放松断言通过。
- I1 已有独立证据沿用 `i1-mysql-validation.md`，没有重跑全套 I1，也不将其冒充 I2 通过。

### 测试

```bash
cd backend
APP_DISABLE_DOTENV=1 .venv/bin/python -m unittest discover -s tests/unit -v
```

结果：**45 项通过**。涵盖原有纯单元与 I2 字段校验、候选构建边界。未安装 pytest，测试复用 unittest。

在私有环境文件配置的 `kindergarten_test_i2` 上：

```bash
.venv/bin/python -m unittest \
  tests.integration.test_class_assignment \
  tests.integration.test_terms_calendar \
  tests.integration.test_i2_concurrency \
  tests.integration.test_i2_remaining -v
```

结果：**56 项通过，14.046 秒，无跳过**。测试使用真实 MySQL 8.4.11 / InnoDB；守卫检查本地端口、白名单库、应用实际目标与测试目标一致，拒绝其他库。主要覆盖：

- 真实分配、现有会话 Me、权限与真实成员、审计字段；不同班首次分配竞争、账号资料／密码重置竞争。
- 班名唯一性竞争、学期重叠竞争与数据库 FK／CHECK／唯一约束。
- 真实库 2026-09-20 周日调休教学、2026-09-25 周五假日休息及持久来源版本 1.11.0。
- 未覆盖年份 unknown、部分例外及撤销，普通库异常 503 且不写配置。
- 重导入保留例外；学期扩大／缩短／起始日移动的默认保留、移出例外、周次与旧快照保留。
- 过期／失效／跨操作者预览、幂等提交、阶段变化。
- 审计写入后触发真实 MySQL 错误并用独立连接检查回滚；并发读写不混合修订。
- 独立 Python 子进程重启后读到同一完整持久日历，禁止库调用仍可读取。
- 有计划阶段阻止既有配置修改，同时允许新班、新非重叠学期及首次分配。

阶段性运行曾失败，已按真实根因修复：限流夹具污染、日期／字段别名序列化、部分更新、测试身份及版本、日历夹具和并发同步等。早期“禁用数据库后全部跳过”的加载检查不计为通过。

前端 `npm run typecheck`、`npm run build` 均通过；构建仍有大于 500 kB 的 chunk 提示，未扩展范围做打包优化。最后一批冲突修复后，直接执行 `npm run typecheck && npm run build` 退出 0；最后构建耗时 620 ms。

本地详细执行日志保存在 `/tmp/kg-next-i2-opencode/` 与 `/tmp/kg-next-i2-validation/`；README 提供可复现命令。部分早期命令使用 tail 管道，因此对应证据以明确输出及后续直接运行结果为准，不把管道退出码当测试成功依据。

### 浏览器（main，Chrome，真实本地 API）

已实际完成：

- 管理员／教师注册登录；待分配教师无班级工作入口。
- 班级、园所资料预览与明确确认，真实字段落库。
- 管理员选择真实班级首次分配；列表变为已分配并移除分配按钮；教师只读页显示本班、园所、表头教师和保育员。
- 真实学期创建与读取，正确 2026-09 月份范围；管理员切换十月后标题与日期行同步且显示学期外。
- 2026-09-05／06 批量例外：预览逐日旧／新状态、原因和来源；刷新服务端 ID 恢复后确认，两天显示管理员例外及原因。
- 班级、园所、日历预览均刷新恢复至对应页；班级改名预览取消后原名称未改变。
- 两个标签页修改同一园所，旧预览被服务端冲突阻止，未覆盖已提交的新值。

已发现并修复：两处后端响应模型导致 500、月历 UTC 日期偏移、切月只更新标题。最后一批修复也已直接复测通过：

- 刷新恢复园所预览时，表单同步恢复候选输入。另一标签页确认新值后，原预览被拒绝，候选输入仍保留；重新预览显示最新基线到原候选的真实差异，再次明确确认后生效。
- 班级编辑详情显示真实账号用户名和姓名缺失提示的只读列表，与手工表头名单独立；无调班入口。
- 表头名单、保育员显式清空后预览列出真实差异，确认后管理员／教师页面显示“未填写”，真实归属保持不变。
- 教师切月后日期行与月份范围同步更新，学期外与未知不混淆；教师仍能读到先前保存的例外原因。
- 最后后端预览响应增加 `candidate`，main 在真实数据库上以现有测试账号直接检查：pending 返回业务候选、applied 的 candidate 为 null、教师读取管理员预览返回 403，全部通过。该检查覆盖最后新增序列化字段；未重复无关 56 项测试。

## 收尾状态

- I2 授权范围已完成，无本轮必需工作遗留。上述检查是直接相关开发验证，不等同于穷尽式安全、容量或生产验收。
- `git diff --check` 通过；暂存区为空，`ARCHITECTURE.md` 无差异。
- 临时 API／Vite 已停止，验证浏览器标签已关闭；MySQL 容器停止后保留测试数据及原卷。原 I1 库未改动。
- 保留的已知提示：前端单个构建 chunk 大于 500 kB；不影响本轮构建，未扩大范围优化。
- 停止于 I2，不自动继续 I3。
