# I4 最新提交窄范围复审（2026-09-24）

基线：`3120a2c4e9ebfac66439445ad54e3c5b01e2300d`，父提交 `7aa67fb`。本轮只审阅与撰写后续提示词，不修改业务代码，不提交或推送。已有未跟踪的旧审阅报告与 `work/` 保留。

## 当前源码发现

### F1 — P1：确认复核取消后，刷新来源仍可绕过旧编辑基线

位置：`frontend/src/views/WeeklyPlanView.vue:687–690`、`:667–672`，以及本次修改的 `enterConfirmReview`。

路径：负责人在 v1 编辑主题但未保存；管理员保存同字段为 v2，且当前有陈旧来源；负责人确认遭遇版本冲突，选择复核最新草稿。此时确认目标/页面为 v2，本地 dirty 仍基于 v1，这是本次修复的正确行为。随后取消确认，点击“刷新到最新来源”：`refreshSources()` 使用展示版本 v2，请求成功生成 v3；`doRefresh()` 又无条件把 dirty 保存基线改成 v3。接着保存本地 v1 输入时 PATCH 携带 v3，覆盖管理员的 v2 主题，没有保存冲突或针对该编辑基线的显式取舍。

源码核对：后端 `weekly_plan_service.py` 的刷新按请求版本验证并递增草稿版本；刷新不会把前端 dirty 主题保存进服务器。因此刷新请求成功并不表示旧 dirty 输入已解决与管理员编辑的冲突。

定向脚本读取当前 Vue script setup，使用现有 TypeScript 转译与 Vue 响应式，API 为受控桩（含版本校验）：复核取消后 base=1/display=2；刷新后 base=3/display=3、local 仍是 v1 输入；随后 PATCH expected=3，服务器主题被本地值替换。脚本退出码 0，证实此路径仍存在。临时复现文件：`/tmp/kg-i4-3120-review.cjs`。这不是实际浏览器或 MySQL 测试。

建议：将“刷新来源”与“显式解决旧编辑冲突”的授权区分开；dirty 基线与展示版本不同时，普通刷新不得无条件推进保存基线。可在刷新前进入现有显式冲突路径，或在成功后保留旧基线并要求显式处理；保留正常同基线刷新及无 dirty 刷新。最终方案由 OpenCode 在现有规格内实现，不扩展产品行为。

### F2 — P2：新增确认目标预览遗漏游戏/区域的实际文本与来源区别

位置：`frontend/src/views/WeeklyPlanView.vue:1724–1743`。

新增预览对户外游戏只显示名称和可选“手工补充”，重点区域只显示名称。主编辑区使用 `form`，dirty 槽位/区域会保留本地整项；同名但目标、指导或来源版本不同的已保存确认目标，无法在确认前被完整审阅。例如本地把同名手工游戏的共用目标改成 L，服务器保存的是 S，主编辑区展示 L，确认预览却仅给游戏名，实际将确认的 S 不可见。同名游戏来自不同日期/版本的路径也无法区分。

建议：确认预览直接从 `confirmTarget.content` 展示槽位/区域的完整业务文本及可辨识的来源信息。复用适合的纯展示 helper；不要从本地 `form` 或最新候选替代快照内容，也不要为来源日期虚构数据。本项依据当前模板、表单和类型源码审阅，未运行 DOM 测试。

## 验证边界

- Codex 已直接读取本次 Git 提交差异和相关当前源码。
- 本轮实际新增执行仅为 F1 定向脚本级复现；未修改业务代码，未重跑已有 typecheck/build 或全面验证。
- 规格记载的既有检查属于历史证据，本报告不将其当成本轮重新执行结果。
- 未启动数据库、容器、API 或 Vite，未安装依赖、读取凭证或部署。

## ChatGPT 协作

已通过 [Codex with ChatGPT 技能](/home/ywyz/.codex/skills/codex-with-chatgpt/SKILL.md)核验当前工作区，并在[本轮 ChatGPT 对话](https://chatgpt.com/g/g-p-6ab3c61acea48191aa003d8a6ab6fb81-kindergarten-manager-next/c/6ab4b626-6c90-83ea-8711-e7b5972c5e39)取得 `STATE: PLAN / c2c_924b / ITERATION: 0` 审阅意见。ChatGPT 从当前源码确认 F1=P1、F2=P2，建议先做这两处窄修复，不重新全面验收、不直接进入 Word 导出实现。

ChatGPT 侧只能读取工作区相对 HEAD 的 diff，不能直接读取 `7aa67fb..3120a2c`；其结论属于当前 HEAD 结果源码复核。提交差异由 Codex 本地直接读取。ChatGPT 在最终结论前读到了本轮草稿并据源码核对，因此两方一致不等于完全盲审。ChatGPT 未执行本地复现脚本；本报告的运行结果来自 Codex。

## 下一步结论

本次修复方向正确，但尚不能宣告“无残留问题”。先按 [OpenCode 定向修复提示词](i4-followup-opencode-prompt-2026-09-24.md)处理 F1/F2，完成对应基本检查后停止，不扩展组件重构或新功能。提示词已撰写，本轮未下发或执行。

修复完成后再准备 I5 Word 导出最小实施规格：以现有导出 Contract、模板验证记录和不可变确认版本为依据，明确文件上限、清理与生成库等未决项，不把建议当用户决定。AI、个人提示词、持久任务仍在首版范围内。
