# OpenCode 提示词：I4 两处残留问题的定向修复

状态：供用户交给 OpenCode 的任务提示词，本轮未启动 OpenCode 或实施修复。基于 `3120a2c` 的复审；协作结论见同目录 `i4-latest-review-2026-09-24.md`。

```text
你在 /home/ywyz/code/kindergarten-manager-next 工作。请完成 I4 的两处窄范围修复；不要开始 Word 导出或 AI 实现。

先读 AGENTS.md，核对 git status 和 HEAD。评审基线为 3120a2c，父提交 7aa67fb；若 HEAD 已推进，只核对这两处问题是否仍存在，不覆盖后续修改。保留所有无关未提交内容，尤其既有 docs/bootstrap/i4-frontend-review-2026-09-23.md 和 work/。不要 reset、clean、stash 或回退他人工作。

按需读取：
- docs/bootstrap/i4-latest-review-2026-09-24.md（F1/F2）
- docs/specs/manual-weekly-plan-confirmation.md §8、§12.2
- frontend/src/views/WeeklyPlanView.vue
- frontend/src/weekly-plan-content.ts 与 types.ts 的相关内容
- backend/app/services/weekly_plan_service.py 的刷新/版本校验，只读核对契约

任务一（F1）：阻止“确认冲突复核 → 取消 → 刷新来源 → 保存”隐式重基。
复现：v1 本地 dirty；另一写者保存同字段为 v2，且有陈旧来源；确认冲突后复核 v2，再取消；普通刷新当前使用展示版本 v2，成功后生成 v3 并无条件将 editBaseVersion 改为 v3；随后本地旧输入 PATCH v3 可覆盖他人编辑。
必须保证：普通刷新来源不是解决旧编辑冲突的授权。dirty 输入的实际基线与服务端展示版本不同时，不得静默推进保存基线；继续使用现有显式冲突处理，让用户明确放弃本地或按服务端基准重提。选择最小且符合现有规格的实现，保持正常同基线刷新与无 dirty 刷新可用，不自动 PATCH/confirm、不丢失输入。别把所有刷新永久禁用作为修复。

任务二（F2）：补全确认目标的游戏/重点区域预览。
预览必须读 confirmTarget.content，而不是 form 或当前候选。户外游戏展示名称、来源类型、共用目标、指导要点、重点指导；重点区域展示名称、上下文及已存的目标、指导、支持策略。展示可用的来源版本/标识，能够区分同名不同来源，日期必须有可靠映射才显示，不新增后端字段、不虚构日期、不从最新来源替换快照文本。空值可辨识，不因字段为空隐藏差异。
允许最小抽取纯展示 helper，避免直接复用依赖 form 的 focusTexts()/focusMeta()。不进行组件拆分或大重构。

保留既有正确行为：
- 确认目标、服务器展示版本、dirty 编辑基线彼此区分。
- 确认冲突复核清空两个 ack、保留 note、无自动请求；再次冲突仍需复核。
- CONFIRM_ACK_REQUIRED 更新最新 facts、重置 ack，不自动重试。
- 确认成功保留 dirty 输入；后续读取发现服务器超前时进入显式冲突。
- 普通保存以真实编辑基线发请求，不能借取消/重开/刷新绕过版本保护。

写入范围：主要为 WeeklyPlanView.vue；必要时仅修改直接相关的展示 helper/定向回归脚本。仅在完成后为 I4 规格 §12 和 readiness 补充真实结果，不重写历史，不改 ARCHITECTURE.md、后端、schema、迁移、API、权限、锁协议或 U1–U5。业务代码由本次 OpenCode 唯一写入，不另启其他实现者。

最小验证：
1. 读取真实组件脚本或使用已有设施，对 F1 的 v1 dirty/v2 review/cancel/refresh/save 路径做回归：没有明确冲突取舍就不能覆盖管理员内容；本地输入仍在。再检查正常同基线刷新、无 dirty 刷新，以及显式冲突两种处理路径。
2. F2 使用同名但目标/指导不同的手工游戏、同名不同来源的日计划游戏及重点区域夹具，检查实际渲染的确认预览来自已保存目标、可见完整文本。纯 script setup 状态测试不能声称验证了模板/DOM。使用现有设施；若无可用渲染环境，明确记录未执行，留给 main 定向浏览器验证。
3. frontend 中 npm run typecheck、npm run build；检查本次 diff。
不新增依赖，不默认跑后端全套、MySQL V1–V14、54 项浏览器矩阵或容量测试。不要为检查自行创建/删除数据库、容器、启动服务、读取 .env 或访问生产；若需要实际浏览器环境，交由 main 在另有明确授权的隔离环境验证。

交付：列出根因、改动文件、实际命令与结果、未执行检查和剩余风险。发现超范围产品问题先记录，不擅自决定。完成上述修复和最小检查后停止，不扩展审查，不自动进入 I5，不提交、不推送、不部署。
```

## 修复之后的下一项

建议准备 I5 Word 导出最小实施规格，先消费已确认周计划的不可变内容版本，结合现有日计划导出约束组织切片。具体先后次序属于实施建议，不能删减既定日计划/周计划导出需求或首版 AI 范围。

导出文件上限、临时文件清理、生产生成库仍需按现有文档明确；有产品影响的未决项只列选项与建议，等待确认。不要在 I4 修复任务中顺带实现导出。
