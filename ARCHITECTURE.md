# Architecture v1

状态：已确认。依据为本次用户逐轮确认的需求与技术决策，以及随后授权的文档落盘任务。

本文记录当前有效的系统级架构设计，不表示产品已经实现或通过验收。业务规则以模块 Contract 为细化依据；原因与备选方案在 ADR 中，未决事项和验证要求在[实施准备清单](docs/bootstrap/architecture-v1-readiness.md)中。未确认建议不作为当前架构事实。

## 首版范围

服务一个园所、多位教师。角色为班级教师与系统管理员。教师注册申请后待分配，管理员分配班级后才能备课；首版一位教师归属一个班级。

核心工作顺序是先日计划、后周计划。教师分日期备课，一位教师创建全班周计划，汇集同班日计划。计划集中保存，支持园内和家中通过公网登录，以及跨设备读取。

必须提供：

- 账号申请、班级分配、学期及上课日配置、班级年级与游戏配置。
- 教师个人 AI 服务配置，以及全部 AI 任务的个人字段生成指导编辑器。
- 日计划原始教案拆分、年龄适宜性调整、其他活动生成、人工编辑确认、原文比较。
- 周计划汇总、游戏自动选取及不足补全、内容生成、版本确认及人工修改保护。
- 固定 Word 模板导出，单份及按日期范围合并导出。
- 可恢复删除、管理员管理权限、操作与 AI 调用日志。
- 数据库持久任务、每日 S3 加密备份和恢复流程。

首版不做多园所、园长审批流、家长或幼儿账号、旧系统数据迁移、其他系统对接、任意 Word 模板上传编辑、Typst 转换。不因旧系统或服务器存在相关组件而复用其业务设计。

## 技术栈与运行结构

| 层 | 已确认选择 |
| --- | --- |
| 前端 | Vue 3、TypeScript、Vite、Element Plus |
| API | Python、FastAPI |
| 数据访问与迁移 | SQLAlchemy 2.x、Alembic |
| 数据库 | MySQL 8.4 / InnoDB；已替代 SQLite 候选 |
| 后台执行 | 同一项目中的独立 Python 工作进程，任务持久化在 MySQL |
| 入口与进程管理 | 现有 Caddy；systemd 管理 API、工作进程与定时备份 |
| 备份目的地 | S3；WebDAV 暂作备用 |

首版采用单 FastAPI 服务进程与一个独立工作进程。工作进程数量不等同于同时执行的 AI 请求数，调用并发上限在后续验证中确定。不新增 Redis、Celery 或消息中间件。

Caddy 提供前端静态文件、HTTPS 和 API 反向代理。API 监听本机内部端口。新系统与现有应用共存，使用独立应用目录、数据目录和端口。部署目标是 `ssh aliyun`，不是此前误给的 `bwh`。

是否复用服务器上已有 MySQL 实例尚待核查；若复用，采用独立数据库、低权限账号、迁移和备份流程，不共享旧系统业务表。

## 模块边界与依赖

模块是同一项目内的职责划分，不是独立微服务。API 与工作进程使用相同业务规则，持久任务通过 MySQL 传递。

| 模块 | 职责 | 主要依赖 |
| --- | --- | --- |
| [用户与班级](docs/modules/identity-and-class.md) | 身份、权限、归属、学期与班级配置 | 数据持久化、日志 |
| [提示词](docs/modules/prompts.md) | 系统字段契约、默认与个人指导、版本适配 | 用户身份、日志 |
| [AI Service](docs/modules/ai-service.md) | 个人服务配置、统一调用、结果校验、失败分类 | 用户权限、提示词、持久任务、日志 |
| [日计划](docs/modules/daily-plans.md) | 日计划内容、原始教案、拆分基准、最终采用内容 | 用户与班级、提示词、AI Service、日志 |
| [周计划](docs/modules/weekly-plans.md) | 汇总、来源关联、候选、人工编辑、确认版本 | 用户与班级、日计划、提示词、AI Service、日志 |
| [Word 导出](docs/modules/word-export.md) | 固定模板、范围筛选、合并、标红 | 日计划、周计划、访问权限 |
| [操作日志](docs/modules/audit-log.md) | 操作及调用记录、管理员查询 | 操作者和对象标识 |

周计划读取日计划；不反向修改日计划。日计划保存与登记相关更新任务在同一事务中完成，周计划任务负责汇总，不将周计划生成逻辑塞入日计划编辑流程。

业务模块决定何时采用 AI 内容；AI Service 与提示词不直接决定正式计划。导出读取业务数据和指定版本，导出的 Word 不是系统数据权威来源。日志不驱动业务状态。

## 数据权威与权限

MySQL 是持久业务数据的权威来源。浏览器 session/localStorage 不是计划、班级配置或 API 密钥的权威存储。

| 数据 | 归属与修改边界 |
| --- | --- |
| 学期、上课日、班级配置 | 管理员统一维护；同班共享年级、区域与户外游戏配置 |
| 日计划 | 创建教师与所属班级；本人及管理员可修改，同班其他教师只读 |
| 周计划 | 同班、同学期、同周仅一份有效计划；负责人编辑确认，管理员可修改但不代替负责人确认 |
| AI 配置与个人指导 | 教师个人；管理员无完整密钥查看入口 |
| 系统字段契约与默认指导 | 系统维护；教师不能删除或改名字段 |
| 历史版本、来源、日志 | 保留来源和操作者，不被候选生成静默覆盖 |

管理员可管理账号、分配班级、查看全园计划、修改和删除教师计划、查看日志。删除可恢复；教师可删除自己的日计划。恢复不能破坏有效周计划唯一性。

调班后历史计划保留原班级与创建者；教师可查看导出自己原班计划但不可修改，不再查看原班他人计划。周计划负责人调班或停用后暂停自动 AI 调用，管理员指定原班教师接管，接管者接受后使用其配置和提示词恢复任务，保留原创建者。

## 关键工作流

### 日计划

教师选择日期，依据学期与上课日配置计算周次、星期并提示非工作日。完整教案由 AI 拆分；活动主题必须读取、存储并导出。活动过程进行年龄适宜性调整，并保留拆分基准用于比较。其他活动可通过 AI 生成。

教师能够预览、修改并确认采用全部日计划 AI 结果。AI 失败保留内容，允许手工编辑、保存和导出。活动过程导出标红以原始教案拆出的活动过程为基准，对比教师最终采用版本。

### 周计划

创建教师填写主题名称，AI 可建议但不自动覆盖。晨间谈话与集体活动名称按日期对应。户外游戏取日计划晨间活动，汇总后自动选取两项集体游戏和一项自选游戏。重点区域可来自任意同班教师、任意日期的集体活动之后的区域、户外或专用室游戏。

引用游戏的指导来自日计划；其他栏目由 AI 根据本周日计划生成。候选不足时列出已有项目并提示缺项，可由 AI 补足名称、目标和指导；补足内容标注来源，只存周计划，不写回日计划。

日计划保存或删除后立即标记周计划待更新，确定性字段更新到待确认内容。最后一次相关保存后等待 30 秒触发 AI，支持立即更新但仍按队列执行。

保留上一份已确认版本、当前人工编辑内容、最新候选。新候选不直接覆盖正在编辑的内容；采用时游戏允许替换，其他人工修改栏目由教师选择是否替换。管理员修改同样形成需要负责人确认的新内容。

执行和确认检查来源版本，旧结果不能作为最新结果确认。来源变化时仍可明确确认当前不完整内容后导出，但不能把旧候选标成基于最新来源生成；须记录缺失或未更新内容。

### 导出

固定使用用户提供的日计划和周计划 Word 结构，保留栏目及主要格式，内容变长自然分页。

日计划按所选日期严格筛选，支持单日、按周、按月、自选范围合并。周计划按上课日期是否与范围有交集选择整份，去重，不裁切周内栏目。

周计划可导出上一份已确认版本，并提示尚未包含更新；也可确认不完整内容后导出，不强制等待 AI 成功。未确认候选不能直接导出。已下载文件是导出时副本，后续修改需重新导出。

## AI、安全与恢复边界

日计划 AI 使用当前教师配置；周计划自动任务使用当前负责人配置与个人提示词，可以离线调用并产生 API 用量。待执行任务使用最新有效配置，执行中任务保持启动时版本，记录实际使用版本。配置变更本身不重新生成已确认计划。

API 密钥加密存储，应用解密密钥与数据库分开保管，页面不完整回显。管理员不可通过产品查看完整密钥；不承诺对服务器最高权限控制者保密。

自定义 API 地址仅允许公网 HTTPS，校验目标与重定向，阻止本机、内网及云元数据地址访问。日志脱敏，不重复记录完整教案、提示词、模型回复或认证头。

鉴权、额度、配置错误暂停；明确临时失败有限重试；发出后结果不明的超时或中断不自动重发。数据库保存未开始任务，进程重启后可恢复；恢复仍须检查当前权限、负责人和提示词状态。

每日一致性备份加密上传 S3，保留最近 30 天，解密所需恢复密钥另行保管。目标为最多丢失最近 24 小时数据、具备一个工作日内恢复服务的流程，必须验证恢复。既有云备份进程不代表本项目已受保护。

## 容量与治理

目标为约 30 人同时在线编辑保存，并允许同时提交 AI 任务；AI 允许排队并显示状态，不要求同时执行 30 个请求。容量、模板兼容性和恢复效果仍待验证。

改变一级技术栈、依赖方向、模块边界、数据权威或通信机制须先形成 ADR 并获得确认。文档落盘不授权安装依赖、创建数据库、复用现有实例、部署或修改服务器。

已确认决策：[技术栈与数据库](docs/adr/0001-stack-and-database.md)、[后台任务与版本](docs/adr/0002-background-tasks-and-versions.md)、[部署安全与恢复](docs/adr/0003-deployment-security-and-recovery.md)。
